{
  "name": "repo-optimizer-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vercel/node": "^5.7.15",
    "typescript": "^5.9.3",
    "vitest": "^4.0.7",
    "zod": "^4.1.12"
  },
  "dependencies": {
    "@upstash/qstash": "^2.10.1",
    "@upstash/redis": "^1.38.0"
  }
}
